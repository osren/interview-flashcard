# 谭成 · 秋招简历 · 通用标准版

> 适用：常规校招前端 · 均衡展示业务交付、AI 工程化与个人项目  
> 技术关键词：React / Ant Design / 小程序·H5 / AI Coding Spec · Agent Skills / CRDT / LLM

---

## 实习经历

**滴滴出行 · 企业版商旅** | 前端开发实习生 | 2026.01 — 2026.07

- 甲子中台交付：独立/主导 **16 条 feature 分支**、**160+ commit**，随 **15 次 release** 上线，主攻机票政策并辐射用车、供应商、会员等模块
- 机火 C 端交付：机票落地分享扩入口与超标预警弹窗；火车落地联系人手机号联动出行人，覆盖增长、管控与代订提效（均已上线）
- AI 规范开发：业务仓接入团队 AI Coding Spec（Rules + Agent 约束 + Skills），大模型在组内规范下协作开发
- 模型 + Skills：用 Cooper / 工作流 Skill / 页面组件脚手架等覆盖「读 PRD → 出方案 → 写代码 → 质检」；人守边界、CR 与发版

---

## 实习产出

### 性能监控与体验优化

**性能监控与体验优化** | PerfMonitor / Omega / Agent Skill | 实习产出

- 监控底座：搭建 `perf-monitor.js`，跨页 `home_to_list_complete`、同页 LCP/FCP、关键接口耗时统一经 Omega 上报；环境 class 兼容 H5 与星河
- 首页/列表：接入 `getHomeData` 耗时；剥离 `privacyPopList` 等拖长 LCP 的路径；列表覆盖「接口返回 / 缓存命中直渲」双路径并修正切日期重渲口径，LCP **1400→1160ms（↓17%）**
- 稳定性：修复 `reportRender` 报错并补最长进入耗时埋点；优化星河底部菜单切换排序逻辑，卡顿约 **↓27%**
- 分析闭环：沉淀 page-performance-analysis Skill（加载/交互/监听三阶段 + P0/P1/P2），与监控指标形成「定位 → 改造 → 验收」闭环

### 商旅 AI 智能监控降噪

**商旅接口错误告警智能降噪（error-triage）** | Node.js / LLM / Prompt 工程 | 实习产出

- 降噪判定落地：告警到达值班前完成智能判定（`should_report` + severity + reason），把噪音挡在 IM/工单之前
- 引擎与 Domain 解耦：HTTP/Kafka 双入口 → 路由 → Prompt 编排 → 网关；业务知识全部下沉 `domains/<team>/`
- 安全底线：宁可误报不可误杀——信息不足默认上报，超时/解析失败统一 P2 上报，漏报率 FN 为评测头号指标
- Skills + Eval 闭环：discover/onboard 降低接入成本；`/judge`/`/eval` 支持 gold 对比与 Prompt 回归；火车 Domain 已接入

---

## 项目经历

**GResume · 智能简历创作与求职管理平台** | React19 / TipTap / CRDT / LLM | 个人项目 | 2025.10 — 2026.06

- 全链路产品：独立设计并实现「编辑 → 模板 → AI 优化 → 协作 → 投递追踪 → 导出」一站式平台，离线优先、登录后云同步
- 编辑与导出：TipTap 富文本 + Zod/RHF 多模块表单；模板 Manifest 与内容解耦；预览与 PDF 共用运行时保证所见即所得
- AI 应用：经 Edge Function 代理 DeepSeek，落地 ATS 结构化诊断（扫描与写入分离）、划词多候选改写、JD 两阶段派生与血缘管理
- 实时协作：基于 Automerge CRDT + Supabase Realtime，支持分享链接、远程光标与离线合并；后续叠加富文本 Yjs 字符级共编
