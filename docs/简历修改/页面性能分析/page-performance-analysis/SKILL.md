---
name: page-performance-analysis
version: 1.0.0
description: 前端页面函数执行耗时性能分析工具
author: Claude
keywords:
  - 性能分析
  - 函数耗时
  - 页面优化
  - 前端性能
  - 性能瓶颈
triggers:
  patterns:
    - 性能分析
    - 函数耗时
    - 页面性能
    - 性能优化
    - 耗时分析
    - 性能瓶颈
    - 页面加载慢
    - 首屏性能
    - 交互性能
---

# 页面函数执行耗时性能分析 Skill

## 使用方式

```
/performance-analysis [页面路径] [--阶段]
```

**支持的命令格式：**
- `/performance-analysis` - 分析当前页面性能
- `/performance-analysis <页面路径>` - 分析指定页面
- `/performance-analysis --load` - 分析加载阶段性能
- `/performance-analysis --interaction` - 分析交互阶段性能
- `/performance-analysis --watch` - 分析 Watch/Computed 耗时

## Skill 描述
对前端项目的指定页面进行深度性能分析，分析页面加载、交互过程中的函数执行耗时，识别性能瓶颈，并提供优化建议。

## 触发场景
- 用户要求分析某个页面的性能
- 用户提到"页面性能分析"、"函数耗时分析"、"性能优化"
- 用户要求对项目进行性能审计

## 分析流程

### Step 0: 上报调用统计（跨工具通用）

在技能执行前上报本次调用，用于团队统计分析：

```bash
# 定位 submodule 路径并调用上报脚本
SCRIPT_DIR=$(find .claude .agent .agents .lingma -name "report-skill.sh" -type f 2>/dev/null | head -1 | xargs dirname)
"$SCRIPT_DIR/report-skill.sh" page-performance-analysis Claude

# 或直接使用相对路径（如果 submodule 在 .claude 目录）
.claude/fe-estrip-ai-coding-spec/common/scripts/report-skill.sh page-performance-analysis Claude

# 工具名称对照：
# Claude Code → Claude
# Codex → Codex
# Cursor → Cursor
# Lingma → Lingma
```

> 说明：
> - 统计数据记录到项目仓库的 `.claude/data/skill-logs/YYYY-MM.jsonl`
> - 技能执行完成后会自动将日志文件一起提交

### 1. 页面识别与准备
- 确认目标页面路径
- 识别页面技术栈（Vue/React/小程序等）
- 了解页面业务场景

### 2. 源码分析维度

#### 2.1 页面加载阶段
- `created`/`mounted` 生命周期函数
- 初始化数据请求函数
- 组件初始化函数
- 路由守卫函数

#### 2.2 用户交互阶段
- 事件处理函数（点击、滚动、输入等）
- 表单提交函数
- 导航跳转函数
- 弹窗/模态框函数

#### 2.3 数据监听阶段
- Watch 监听器
- Computed 计算属性
- 数据订阅函数
- 状态管理更新函数

### 3. 耗时评估方法

#### 3.1 网络请求耗时评估
```javascript
// 根据接口类型评估
- 简单查询接口: 100-300ms
- 复杂查询接口: 300-600ms
- 聚合数据接口: 500-1000ms
- 大数据量接口: 800-2000ms
```

#### 3.2 数据处理耗时评估
```javascript
// 根据操作复杂度评估
- 简单数据赋值: 1-10ms
- 数组遍历处理: 10-50ms
- 复杂计算逻辑: 50-100ms
- 递归/循环处理: 100-500ms
```

#### 3.3 渲染更新耗时评估
```javascript
// 根据组件复杂度评估
- 简单状态更新: 5-20ms
- 列表渲染更新: 20-100ms
- 复杂组件重渲染: 50-200ms
- 整页面重渲染: 100-500ms
```

### 4. 性能瓶颈识别

#### 4.1 串行请求瀑布流
识别多个请求的依赖关系，分析是否存在可并行的请求。

#### 4.2 频繁触发问题
识别 Watch、事件监听器的触发频率，分析是否存在过度触发。

#### 4.3 防抖节流问题
识别防抖、节流配置，分析是否合理。

#### 4.4 重复请求问题
识别相似参数的重复请求，分析缓存策略。

### 5. 优化建议模板

#### P0 级（立即优化）
- 影响用户体验的关键问题
- 容易实施且效果显著

#### P1 级（中期优化）
- 性能提升明显
- 需要一定开发成本

#### P2 级（长期优化）
- 架构层面的优化
- 需要较大改动

## 输出报告格式

### 报告结构
1. **页面概述**
   - 页面名称和路径
   - 技术栈
   - 业务场景

2. **函数执行耗时排序**
   - 第一梯队（高耗时）
   - 第二梯队（中等耗时）
   - 第三梯队（低耗时）

3. **性能瓶颈分析**
   - 关键路径分析
   - 瀑布流图
   - 耗时分解

4. **优化建议**
   - 优先级排序
   - 具体实施方案
   - 预期收益

5. **性能指标对比**
   - 当前指标
   - 优化目标
   - 预期改进

## 使用示例

### 分析酒店首页
```
请分析酒店首页的函数执行耗时性能
```

### 分析订单列表页
```
对订单列表页进行性能分析，重点关注列表渲染和筛选功能
```

### 分析详情页
```
分析酒店详情页的性能，特别关注图片加载和数据请求
```

## 分析工具

### 时间估算表
| 操作类型 | 耗时范围 | 影响因素 |
|---------|---------|---------|
| 网络请求（简单） | 100-300ms | 网络状况、数据量 |
| 网络请求（复杂） | 300-800ms | 接口逻辑、数据量 |
| 数据处理（简单） | 1-50ms | 数据量、复杂度 |
| 数据处理（复杂） | 50-200ms | 算法复杂度 |
| DOM操作（简单） | 5-20ms | 元素数量 |
| DOM操作（复杂） | 20-100ms | 嵌套层级 |
| 组件渲染 | 10-100ms | 组件复杂度 |

### 性能指标基准
| 指标 | 良好 | 需优化 | 差 |
|------|------|--------|-----|
| FCP | < 1.8s | 1.8-3.0s | > 3.0s |
| LCP | < 2.5s | 2.5-4.0s | > 4.0s |
| TTI | < 3.8s | 3.8-7.3s | > 7.3s |
| FID | < 100ms | 100-300ms | > 300ms |
| CLS | < 0.1 | 0.1-0.25 | > 0.25 |

## 注意事项

1. **上下文理解**
   - 理解页面业务场景
   - 考虑不同入口路径
   - 关注特殊业务逻辑

2. **技术栈适配**
   - Vue: 生命周期、watch、computed
   - React: hooks、effect、memo
   - 小程序: 页面生命周期、组件通信

3. **数据来源**
   - 网络请求
   - 本地存储
   - 状态管理
   - URL参数

4. **交互场景**
   - 首次加载
   - 页面返回
   - 参数变化
   - 用户操作
